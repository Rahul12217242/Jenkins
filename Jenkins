pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo '🌿 Building the project...'
                // Put your build commands here, e.g.:
                // sh 'mvn clean install' or 'npm install' etc.
            }
        }

        stage('Done') {
            steps {
                echo '✨ Build complete. Pipeline is alive and kicking!'
            }
        }
    }
}
